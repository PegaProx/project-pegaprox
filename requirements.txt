# PegaProx Requirements
# Install with: pip install -r requirements.txt

# === Core (Required) ===
flask>=2.3.0
flask-cors>=4.0.0
flask-sock>=0.6.0
flask-compress>=1.14
# MK May 2026 — bumped requests + urllib3 floors to dodge CVE-2026-25645
# and friends. Older versions still work in a pinch but pip-audit flags them.
requests>=2.33.0
urllib3>=2.0.0

# === Encryption & Security ===
# NS May 2026 — cryptography 46.0.6/.0.7 patches CVE-2026-34073 + -39892,
# pyopenssl 26.x patches CVE-2026-27448 + -27459. argon2-cffi unchanged.
cryptography>=46.0.7
pyopenssl>=26.0.0
argon2-cffi>=23.0.0

# === SSH & Console ===
paramiko>=3.0.0
websockets>=11.0
websocket-client>=1.6.0

# === Performance (Recommended) ===
gevent>=23.0.0
gevent-websocket>=0.10.0

# === Two-Factor Auth ===
pyotp>=2.9.0
# qrcode[pil] pulls Pillow; bump floor so CVE-2026-40192 is dodged.
qrcode[pil]>=7.4.0
pillow>=12.2.0
# WebAuthn / FIDO2 hardware tokens (YubiKey, Nitrokey, Touch ID, Windows Hello).
# Optional at runtime — feature is auto-disabled if import fails.
fido2>=1.1.0

# === LDAP Auth ===
ldap3>=2.9.0

# === OIDC JWT Verification ===
# >=2.12 patches CVE-2026-32597
PyJWT[crypto]>=2.12.0

# === ESXi Migration (Optional) ===
pyvmomi>=8.0.0

# === XCP-ng Integration (Optional) ===
XenAPI>=23.14.0
